# For demo use only. Replace with a real cert in production.
apiVersion: v1
kind: Secret
metadata:
  name: ts-apparatus-tls
  namespace: echo
type: kubernetes.io/tls
data:
  tls.crt: REPLACE_WITH_BASE64_CERT
  tls.key: REPLACE_WITH_BASE64_KEY
